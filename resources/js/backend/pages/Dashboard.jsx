import React from 'react';

function Dashboard(props) {
    // const apiUrl = import.meta.env.APP_URL;
    // console.log(apiUrl)
    return (
        <div>Dashboard</div>
    );
}

export default Dashboard;
